version: "3.9"
services:
  database:
    image: postgres:14
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
  publicapi:
    build:
      context: ./publicapi
    ports:
      - "8080:8080"
      - "8081:8081"
